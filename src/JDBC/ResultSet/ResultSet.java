package JDBC.ResultSet;

public class ResultSet {
    /**
     ResultSet:结果集对象，封装查询结果
        Boolean next():游标向下移一行，判断这一行是否有数据，如果没有返回false，如果有返回true
        get数据类型(参数):获取数据  ex：int getInt(), String getString();
            int 代表列的编号，从1开始
            String：代表列的名称
     注意：
        *使用步骤（循环）：
          1.游标向下移动一行
          2。判断是否有数据
          3.获取数据
     */
    
}
